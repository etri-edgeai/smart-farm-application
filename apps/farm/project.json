{
  "name": "farm",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/farm/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/webpack:webpack",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/farm",
        "main": "apps/farm/src/main.ts",
        "tsConfig": "apps/farm/tsconfig.app.json",
        "assets": ["apps/farm/src/assets"],
        "target": "node",
        "compiler": "tsc",
        "webpackConfig": "apps/farm/webpack.config.js",
        "isolatedConfig": true,
        "babelUpwardRootMode": true
      },
      "configurations": {
        "production": {
          "optimization": true,
          "extractLicenses": true,
          "inspect": false,
          "fileReplacements": [
            {
              "replace": "apps/farm/src/environments/environment.ts",
              "with": "apps/farm/src/environments/environment.prod.ts"
            }
          ]
        }
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "options": {
        "buildTarget": "farm:build"
      },
      "configurations": {
        "production": {
          "buildTarget": "farm:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/farm/**/*.ts"]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/apps/farm"],
      "options": {
        "jestConfig": "apps/farm/jest.config.ts",
        "passWithNoTests": true
      }
    }
  },
  "tags": []
}
