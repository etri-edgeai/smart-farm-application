import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'kr.farmcloud.connect',
  appName: 'ConnectOn',
  webDir: '../../dist/web',
  bundledWebRuntime: false
};

export default config;
